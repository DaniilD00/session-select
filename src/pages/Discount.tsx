import StayTuned from "./StayTuned";

const Discount = () => {
  // Reuse the waitlist experience so both /discount and /launch share the same flow
  return <StayTuned />;
};

export default Discount;
